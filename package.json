{
  "name": "casa-del-yeso-artistico",
  "private": true,
  "version": "0.1.0",
  "description": "Ecosistema digital para La Casa del Yeso Artístico",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "lint": "npm run lint --workspace=apps/web"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
